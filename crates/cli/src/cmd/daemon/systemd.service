[Unit]
Description=Crabtalk Daemon
After=network-online.target

[Service]
Type=simple
WorkingDirectory={home_dir}
Environment=PATH={path}
ExecStart={binary} daemon start -v
Restart=always
RestartSec=3

[Install]
WantedBy=default.target
