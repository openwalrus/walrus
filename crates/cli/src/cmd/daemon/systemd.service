[Unit]
Description={description}
After=network-online.target

[Service]
Type=simple
WorkingDirectory={home_dir}
Environment=PATH={path}
ExecStart={binary} daemon run -v
Restart=always
RestartSec=3

[Install]
WantedBy=default.target
