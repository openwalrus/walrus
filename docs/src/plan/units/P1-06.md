# P1-06: Update Agent Struct (skill_tags)

## Goal

Add `skill_tags` to Agent for skill selection by the Runtime.

## Depends On

P1-00 (Agent fields use `CompactString` and `SmallVec` after migration).

## Design Decisions

- `skill_tags: SmallVec<[CompactString; 4]>`, defaults to empty.
- Builder method: `skill_tag(mut self, tag: impl Into<CompactString>) -> Self`.

## Files

- **MODIFY** `crates/core/src/agent.rs` — add field + builder method

## Tests

- `agent_skill_tags_default_empty`
- `agent_skill_tag_builder` — verify chaining works

## Done When

- [ ] `cargo check --workspace` and `cargo test --workspace` pass
- [ ] Existing Agent usage still compiles (Default covers new field)
